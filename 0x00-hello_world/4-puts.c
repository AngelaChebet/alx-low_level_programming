#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 * MyString to print out programming is like building a multilingual puzzle
 */
int main(void)
{
	char MyString[] = "\"Programming is like building a multilingual puzzle";

	puts(MyString);

	return (0);
}

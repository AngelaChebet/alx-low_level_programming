#include <stdio.h>

/**
 * main - prints all single digits in base 10
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 1 ; i < 10 ; i++)
{
		printf("%d", i);
}
		putchar('\n');

	return (0);
}
